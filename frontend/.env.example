# Absolute URL to GraphQL (browser calls this — use backend origin in production).
# In dev, leave unset to use Vite proxy at /graphql.
VITE_GRAPHQL_URL=http://localhost:4000/graphql

# Customer is chosen on the login screen and stored in localStorage (not via env).
